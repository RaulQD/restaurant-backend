
export class UserModel {
    // static async getUserByEmail({ email }) {
    //     try {
    //         cosnt[user] = await conn.query('SELECT * FROM users WHERE email = ?', [email]);
    //     } catch (error) {

    //     }

    // }
}